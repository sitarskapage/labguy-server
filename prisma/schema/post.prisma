model Post {
  id   Int     @id @default(autoincrement())
  html String?

  general GeneralSection @relation(fields: [generalSectionId], references: [id])
  author  User           @relation(fields: [userId], references: [id])

  generalSectionId Int
  userId           Int
}
